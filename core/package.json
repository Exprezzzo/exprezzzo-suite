{
  "scripts": {
    "dev": "next dev",
    "build": "npm run validate-env && next build",
    "start": "next start",
    "lint": "next lint --fix",
    "type-check": "tsc --noEmit",
    "validate-env": "node scripts/validate-env.js",
    "prebuild": "npm run lint && npm run type-check",
    "test": "jest --passWithNoTests",
    "test:e2e": "cypress run",
    "analyze": "ANALYZE=true next build",
    "deploy:preview": "firebase hosting:channel:deploy preview",
    "deploy:prod": "npm run build && firebase deploy --only hosting",
    "deploy:functions": "cd functions && npm run build && firebase deploy --only functions",
    "deploy:all": "npm run deploy:functions && npm run deploy:prod",
    "emulators": "firebase emulators:start",
    "clean": "rm -rf .next out node_modules/.cache"
  }
}
